import React from "react";
interface SpaceProps {
    width?: React.CSSProperties["width"];
    height?: React.CSSProperties["height"];
}
declare const Spacer: ({ width, height }: SpaceProps) => any;
export default Spacer;
